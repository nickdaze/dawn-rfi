# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.shopify
/node_modules
run-lighthouse.sh
test-lighthouse.sh
/lighthouse-reports
*.zip
.gstack/

# Local secrets — NEVER commit. Add real values only to .env (ignored below).
.env
.env.*
!.env.example

# Internal-linking rollback dumps (local only, contain full product descriptions)
scripts/rollback-internal-links-*.json

# Separate Shopify app project scaffolded inside this repo by accident — ignore.
# (Consider moving it out to its own directory.)
/seo-content-drafting/
